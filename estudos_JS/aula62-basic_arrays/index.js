/* const nomes = ['matheus', 'maria', 'santos'];
// const nome = [...nomes]; copia o array original
const removeUltimo = nomes.pop(); // retira o ultimo indice
const removeComeco = nomes.shift(); // retira o primeiro indice 
const adicione = nomes.push('joao'); // adiciona um indice no final
const adiciona1 = nomes.unshift('otavio'); // adiciona um indica no começo

console.log(nomes, removeUltimo); */

/* const nomes = ['matheus', 'maria', 'santos', 'otavio', 'souza', 'joana'];
const novo = nomes.slice(1, 3) */

/* const nome = 'matheus santos souza';
const nomes = nome.split(' ') */

const nomes = ['matheus', 'santos', 'souza'];
const nome = nomes.join(' ');

console.log(nome);